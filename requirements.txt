# Streamlit Community Cloud reads this file. Kept in sync with pyproject.toml
# by hand: any dep bumped there should be bumped here too.

# Install this repo as an editable package so `from maison_concierge...` works.
# Streamlit Cloud runs `pip install -r requirements.txt` — without this line
# the src-layout package is on disk but not importable.
-e .

anthropic>=0.40.0
langgraph>=0.2.0
langchain-core>=0.3.0
pydantic[email]>=2.7.0
pydantic-settings>=2.4.0
python-dotenv>=1.0.0

chromadb>=0.5.0
sentence-transformers>=3.0.0
scikit-learn>=1.4.0
lightgbm>=4.3.0
shap>=0.45.0
pandas>=2.2.0
pyarrow>=15.0.0
joblib>=1.4.0
pyyaml>=6.0
rank-bm25>=0.2.2

structlog>=24.1.0
tenacity>=8.2.0

streamlit>=1.35.0
altair>=5.3.0
